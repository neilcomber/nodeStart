// Globals - Because there in no window/ dom!!

// __dirname = path to current directory
// __filename = filename
// require = function to use modules (CommnJS)
// module = infor about current module (file your using)
// process = info about enviroment where the program is being executed (process.env) - when you deploy your app your enviroment will be different!

// I Will Always have access to those variables!!!

console.log(__dirname)

// Still have Set INterval and Set Timeout

setInterval(()=>console.log('here is the interval'), 2000)